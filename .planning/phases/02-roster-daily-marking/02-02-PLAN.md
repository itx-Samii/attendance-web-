---
phase: 02-roster-daily-marking
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/DashboardLayout.tsx
  - src/app/dashboard/page.tsx
  - src/app/dashboard/attendance/page.tsx
autonomous: true
requirements: [ATT-01, ATT-02, ROST-01, ROST-02, ROST-03, DASH-03]
user_setup: []

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "Dashboard layout renders premium sidebar with active visual links"
    - "Sign Out button clears session cookie and redirects user to /login"
    - "Roster page dynamically queries GET /api/students?classId=XXX on dropdown selection"
    - "Submitting the attendance form sends correct records array to POST /api/attendance"
  artifacts:
    - "src/components/DashboardLayout.tsx"
    - "src/app/dashboard/page.tsx"
    - "src/app/dashboard/attendance/page.tsx"
  key_links:
    - "Navigation items smoothly transition page paths using standard Next.js Link elements"
---

<objective>
Design and develop the premium, highly interactive front-end dashboard panels including the responsive sidebar navigation layout, the dynamic analytics overview hub, and the ultimate daily student attendance marking sheet.

Purpose: Creates a gorgeous, user-friendly portal matching school requirements to easily mark, track, and review student statuses in real-time.
Output: Shared sidebar navigation layouts, analytics charts, class dropdown filters, dynamic stats counters, and interactive status markers.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
@src/lib/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Shared Dashboard Sidebar & Header Layout</name>
  <files>src/components/DashboardLayout.tsx</files>
  <read_first>src/app/globals.css</read_first>
  <action>
    Create a reusable client component `src/components/DashboardLayout.tsx` that wraps all administrative sub-pages.
    - **Sidebar Panel**: Renders a glassmorphic sidebar featuring:
      - App logo & Title: "Aura ERP"
      - Profile badge (resolving active teacher's name dynamically from session or client GET `/api/auth/user`)
      - Navigation links:
        1. **Overview Dashboard** (links to `/dashboard`)
        2. **Mark Attendance** (links to `/dashboard/attendance`)
      - Integrated sun/moon `ThemeToggle` button
      - **Sign Out Button**: Triggers a POST call to `/api/auth/logout` and redirects immediately to `/login` with visual exit feedback.
    - **Layout Column**: Side-by-side flex layout on desktop, converting to clean toggle/collapsible layout on mobile viewport sizes.
  </action>
  <verify>Access the dashboard route, verify layout mounts smoothly, Sun/Moon switcher reacts instantly, and clicking Logout redirects safely to /login.</verify>
  <acceptance_criteria>
    - Sidebar renders successfully with glassmorphism classes and transitions
    - Dynamic user profile fetches name from credentials profile
    - Clicking 'Logout' successfully clears the cookie session and triggers redirect
  </acceptance_criteria>
  <done>Shared dashboard layout navigation frame is fully complete.</done>
</task>

<task type="auto">
  <name>Task 2: Build Main Analytics Dashboard Overview Hub</name>
  <files>src/app/dashboard/page.tsx</files>
  <read_first>src/components/DashboardLayout.tsx</read_first>
  <action>
    Develop the core dashboard overview panel at `src/app/dashboard/page.tsx`.
    - Wrap the entire page inside `<DashboardLayout>`.
    - **Welcome Card**: A premium hero block greeting the teacher ("Welcome Back, Prof. Aura!").
    - **Dynamic Overview Statistics Grid**: Card widgets displaying real-time metrics:
      1. **Total Classes**: 4 Active Sections
      2. **Total Students**: 40 Enrolled
      3. **Today's Attendance Rate**: Calculate dynamically from saved `attendance.json` records for today, or fallback to mock rate (e.g. `96.4%`) if empty.
      4. **Parent SMS Dispatches**: Counter displaying total notifications logged today (0 in Phase 2).
    - **Roster Shortcuts**: Direct quick-actions to launch marking for individual classes (e.g. Grade 9-A, Grade 10-A).
  </action>
  <verify>Access /dashboard, confirm beautiful statistics cards mount successfully and load without flickering.</verify>
  <acceptance_criteria>
    - Welcome panel loads the active teacher profile dynamically
    - Metric widgets render properly using globals.css transition wrappers
    - responsive class selectors guarantee perfect layout display across mobile screens
  </acceptance_criteria>
  <done>Overview metrics page is successfully deployed.</done>
</task>

<task type="auto">
  <name>Task 3: Build Daily Student Attendance Marking Sheet</name>
  <files>src/app/dashboard/attendance/page.tsx</files>
  <read_first>src/components/DashboardLayout.tsx</read_first>
  <action>
    Create the attendance marking workspace at `src/app/dashboard/attendance/page.tsx`:
    - Wrap the page in `<DashboardLayout>`.
    - **Filters Block**: Provide a Class Selector Dropdown and Date Picker. On select, trigger state changes to query GET `/api/students?classId=XXX` and GET `/api/attendance?classId=XXX&date=YYYY-MM-DD`.
    - **Roster Stats Overview**: Renders dynamic real-time badge counters summarizing current marking selection (e.g. Present: 8 | Absent: 1 | Late: 1 | Leave: 0) to guide teachers before posting.
    - **Roster Table Sheet**:
      - Display columns: **Roll Number**, **Name**, **Status Selector**, and **Remarks**.
      - **Status Selector Chips**: Create an elegant, compact button group for each student containing:
        - **P** (Present - soft green badge)
        - **A** (Absent - soft red badge)
        - **L** (Late - soft orange badge)
        - **LV** (Leave - soft cyan badge)
      - Implement direct keyboard selection shortcuts or smooth transitions on selections.
    - **Submit Footer**: Center a prominent "Save Roster Attendance" button. Clicking triggers a POST call to `/api/attendance` with JSON pay structures, showing a gorgeous floating success toaster on success.
  </action>
  <verify>Select Class "Grade 9 - Section A" and date, select statuses for students, check counter badges increment dynamically, and verify clicking Save triggers a successful API post.</verify>
  <acceptance_criteria>
    - Dropdown retrieves classrooms dynamically from `/api/classes`
    - Changing class query fetches matching student directories and populates the table
    - Roster statuses counters (P/A/L/LV) update instantly on click
    - Save action sends atomic upsert payload and outputs a confirmation alert
  </acceptance_criteria>
  <done>Roster daily marking dashboard sheet is 100% complete and fully verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `npm run build` to confirm zero TS or compilation warnings
- [ ] Sign in and load `/dashboard` verifying layout rendering
- [ ] Load `/dashboard/attendance` and verify class selection fetches student array
- [ ] Mark attendance, verify counter badges increment, and click Save confirming persistence
</verification>

<success_criteria>
- Navigation layout and responsive sidebar operate smoothly with instant theme transitions
- Selecting classrooms retrieves student arrays dynamically
- Marking statuses triggers immediate client statistics counter updates
- POST submits records atomically to attendance.json, verifying zero duplicates on reload
</success_criteria>

<output>
After completion, create `.planning/phases/02-roster-daily-marking/02-02-SUMMARY.md`
</output>
