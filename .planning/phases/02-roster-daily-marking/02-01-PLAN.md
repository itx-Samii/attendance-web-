---
phase: 02-roster-daily-marking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/app/api/classes/route.ts
  - src/app/api/students/route.ts
  - src/app/api/attendance/route.ts
autonomous: true
requirements: [ATT-04, ROST-04]
user_setup: []

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "GET /api/classes returns mock classroom records with classId and name"
    - "GET /api/students?classId=C1 returns array of students assigned to class C1"
    - "POST /api/attendance saves student status records atomically to attendance.json"
    - "GET /api/attendance?classId=C1&date=2026-05-24 returns saved student records"
  artifacts:
    - "src/app/api/classes/route.ts"
    - "src/app/api/students/route.ts"
    - "src/app/api/attendance/route.ts"
  key_links:
    - "REST API endpoints link directly to db.ts readTable/writeTable operations"
---

<objective>
Initialize the classrooms and students databases with high-quality mock data automatically, and construct HTTP REST API routes to retrieve classrooms, filter student directories by class ID, and read/write daily attendance sheets atomically.

Purpose: Establishes a robust relational backend matching school requirements to serve data instantly to front-end roster views.
Output: Relational seeds inside local storage, GET classes/students, and GET/POST attendance REST endpoints.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Automatic Database Seeding in db.ts</name>
  <files>src/lib/db.ts</files>
  <read_first>src/lib/db.ts</read_first>
  <action>
    Modify `src/lib/db.ts` to include high-quality, professional mock data seeding inside the `initDatabase()` method.
    - If `classes.json` is empty, seed 4 classroom records:
      1. Class ID: `9a`, Name: `Grade 9 - Section A`
      2. Class ID: `9b`, Name: `Grade 9 - Section B`
      3. Class ID: `10a`, Name: `Grade 10 - Section A`
      4. Class ID: `10b`, Name: `Grade 10 - Section B`
    - If `students.json` is empty, seed 10 mock student records per class (40 total students). Each student record must contain:
      1. `rollNumber`: unique alphanumeric string (e.g. `9A-01`, `10B-08`)
      2. `name`: a realistic, professional full name
      3. `parentPhone`: valid phone number structure (e.g. `+15550100001` or `+923330000001`) for future Twilio integration
      4. `classId`: linking to the active classroom ID
    Ensure `initDatabase` executes automatically on server bootstrap or when routes are first queried.
  </action>
  <verify>Run build or local server, inspect process.cwd()/data/classes.json and students.json to verify they are populated with the seeded array.</verify>
  <acceptance_criteria>
    - `src/lib/db.ts` contains seeded mock classroom entries (Grade 9 & 10)
    - `src/lib/db.ts` contains mock student entries matching respective Class IDs
    - Seeding is triggered safely only when tables are blank to prevent overwriting existing custom inputs
  </acceptance_criteria>
  <done>data/classes.json and data/students.json are successfully seeded on disk.</done>
</task>

<task type="auto">
  <name>Task 2: Build Classrooms and Students REST API routes</name>
  <files>src/app/api/classes/route.ts, src/app/api/students/route.ts</files>
  <read_first>src/lib/db.ts</read_first>
  <action>
    Create two new Next.js App Router API route handlers:
    1. `src/app/api/classes/route.ts`:
       - GET: Retrieve full array from `classes.json` using `readTable('classes')`. Run `initDatabase()` first to ensure files exist.
    2. `src/app/api/students/route.ts`:
       - GET: Check search query params for `classId`.
       - If `classId` is missing, return a 400 Bad Request error.
       - If `classId` is present, read all students using `readTable('students')` and return only those matching the requested `classId`.
    Implement robust error handling returning standard JSON `{ error: message }` responses.
  </action>
  <verify>Query GET /api/classes and GET /api/students?classId=9a via curl or fetch checks to verify valid status codes and matching JSON profiles.</verify>
  <acceptance_criteria>
    - GET `/api/classes` returns status 200 and seeded classrooms
    - GET `/api/students?classId=9a` returns status 200 and students filtered by class `9a`
    - GET `/api/students` (without params) returns status 400 with helpful JSON error
  </acceptance_criteria>
  <done>Classrooms and students directory APIs are fully functional and type-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Develop Daily Attendance GET/POST REST APIs</name>
  <files>src/app/api/attendance/route.ts</files>
  <read_first>src/lib/db.ts</read_first>
  <action>
    Create the primary daily attendance REST API route at `src/app/api/attendance/route.ts`:
    - GET: Query parameters: `classId` (string) and `date` (string, ISO format YYYY-MM-DD).
      - If either param is missing, return 400 Bad Request.
      - Read all records from `attendance.json` using `readTable('attendance')`.
      - Filter and return records matching the requested `classId` and `date`. If none exist, return `[]`.
    - POST: Parse JSON body containing:
      - `classId` (string)
      - `date` (string, YYYY-MM-DD)
      - `records` (array of: `{ rollNumber: string, status: 'Present'|'Absent'|'Late'|'Leave', remarks?: string }`)
      - Validate parameters. Return 400 if validation fails.
      - Read existing attendance database.
      - Upsert (insert or overwrite) the daily records matching the requested `classId` and `date` to avoid duplicate entries for the same day and class.
      - Save the updated array atomically back to `attendance.json` using `writeTable('attendance')`.
  </action>
  <verify>Send mock POST request to `/api/attendance` with JSON payload, followed by a GET query to `/api/attendance?classId=9a&date=2026-05-24` verifying upserts work perfectly.</verify>
  <acceptance_criteria>
    - GET `/api/attendance` filters records correctly by class and date
    - POST `/api/attendance` upserts student statuses atomically to `attendance.json`
    - Double submissions on the same class/date update existing items instead of duplicating them
  </acceptance_criteria>
  <done>Daily attendance read/write endpoints are successfully verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `npm run build` to confirm zero TS or compilation warnings
- [ ] Send GET to `/api/classes` verifying seeded output
- [ ] Send GET to `/api/students?classId=10a` verifying filtered student directory
- [ ] Send POST/GET cycle to `/api/attendance` validating records persistence
</verification>

<success_criteria>
- All 3 API endpoints complete and fully functional
- Mock classrooms database (4 classes) and mock students database (40 students) populated automatically
- Zero TS or Turbopack warnings introduced
- Double POST requests to `/api/attendance` perform reliable upserts
</success_criteria>

<output>
After completion, create `.planning/phases/02-roster-daily-marking/02-01-SUMMARY.md`
</output>
