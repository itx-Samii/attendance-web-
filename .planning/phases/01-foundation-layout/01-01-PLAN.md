---
phase: 01-foundation-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, src/components/ThemeProvider.tsx, src/components/ThemeToggle.tsx]
autonomous: true
requirements: [DASH-03, AUTH-01]

must_haves:
  truths:
    - "Landing page loads with a professional and clean enterprise light theme layout by default."
    - "Dynamic dark/light mode toggle operates seamlessly without layout shifts or flashes."
    - "Custom vanilla CSS variables adjust colors instantly when the theme is toggled."
  artifacts:
    - path: "src/app/globals.css"
      provides: "Enterprise styling system and bespoke CSS color variables"
      contains: "--background-default"
    - path: "src/components/ThemeProvider.tsx"
      provides: "Light/Dark theme React context and cookie synchronization"
    - path: "src/components/ThemeToggle.tsx"
      provides: "Premium glassmorphic switcher toggle component"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/ThemeProvider.tsx"
      via: "context provider wrapping kids"
      pattern: "<ThemeProvider"
---

<objective>
Initialize the Next.js App Router codebase, configure our global responsive styling engine using bespoke enterprise light theme design tokens (with dark mode support), and implement client/server cookie-based color theme switching.

Purpose: Establishes a highly premium, clean visual base for all dashboards and ensures smooth style transitions.
Output: Active Next.js project and operational styling layout controller.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-layout/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js Workspace</name>
  <files>package.json, tsconfig.json, next.config.ts, src/app/page.tsx, src/app/layout.tsx</files>
  <action>
    Run create-next-app inside the workspace directory using clean, non-interactive options:
    `npx -y create-next-app@latest ./ --typescript --eslint --no-tailwind --app --src-dir --import-alias "@/*" --use-npm --yes --disable-git`
    This will initialize the App Router framework without polluting the workspace with Tailwind or default Git conflicts.
  </action>
  <verify>Run `npm run build` to confirm the greenfield app builds without errors.</verify>
  <done>Next.js structure created successfully and package.json contains React and Next.js dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Build Design Tokens and CSS Layout system</name>
  <files>src/app/globals.css</files>
  <read_first>src/app/globals.css</read_first>
  <action>
    Replace the default globals.css with our premium design framework. It must define light/dark styling variables under the root selector:
    - Light Mode (default): `--background-default: #f8fafc`, `--surface-card: #ffffff`, `--text-primary: #0f172a`, `--border-dim: #e2e8f0`, `--color-accent: #3b82f6` (enterprise blue).
    - Dark Mode: `--background-default: #0b0f19` (deep space navy), `--surface-card: #151d30`, `--text-primary: #f8fafc`, `--border-dim: #1e293b`, `--color-accent: #60a5fa`.
    Include responsive CSS grid columns, modern font-family properties (Outfit/Inter fallback), layout margins, and glassmorphic micro-animations.
  </action>
  <verify>Confirm globals.css has definitions for `--background-default` and `.theme-dark` color variable overrides.</verify>
  <done>globals.css has been completely rewritten into our enterprise visual design token system.</done>
</task>

<task type="auto">
  <name>Task 3: Develop Theme Provider and Switcher Component</name>
  <files>src/components/ThemeProvider.tsx, src/components/ThemeToggle.tsx</files>
  <action>
    1. Create ThemeProvider inside `src/components/ThemeProvider.tsx` using React Context. Persist theme selection in both localStorage and a session cookie (`theme=light` or `theme=dark`) so server components can access preference.
    2. Create a dynamic ThemeToggle button in `src/components/ThemeToggle.tsx` using inline SVGs representing sun and moon icons, featuring a smooth rotation animation on toggle.
  </action>
  <verify>Ensure the components use `'use client'` where React client state hooks are active.</verify>
  <done>ThemeProvider and ThemeToggle are fully coded and functional.</done>
</task>

<task type="auto">
  <name>Task 4: Integrate Provider and layout view</name>
  <files>src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>src/app/layout.tsx, src/app/page.tsx</read_first>
  <action>
    1. Edit `src/app/layout.tsx` to read the `theme` cookie server-side using Next.js `cookies()` helper. Inject the default theme class (`theme-light` or `theme-dark`) directly into the `&lt;html&gt;` tag before rendering children to guarantee zero layout flash.
    2. Wrap layout children in our `ThemeProvider`.
    3. Modify `src/app/page.tsx` to serve as a beautiful, premium visual greeting page with card panels and the theme switch button displayed in the header.
  </action>
  <verify>Run `npm run build` and launch a local dev test server to verify compilation.</verify>
  <done>ThemeProvider wraps root layout correctly and greeting page renders with working theme toggle controls.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` compiles with zero warnings or errors.
- [ ] Theme toggle transitions background color instantly.
- [ ] No layout flicker/flash when reloading landing page.
</verification>

<success_criteria>
- Core Next.js App Router codebase scaffolded.
- Bespoke custom CSS color variables system initialized.
- Light/Dark theme switching correctly synced via localStorage and cookies.
- Landing template represents a highly premium enterprise look and feel.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-layout/01-01-SUMMARY.md`
</output>
