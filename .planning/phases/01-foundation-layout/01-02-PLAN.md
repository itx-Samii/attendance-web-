---
phase: 01-foundation-layout
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/lib/db.ts, src/lib/authConfig.ts, src/middleware.ts, src/app/api/auth/login/route.ts, src/app/api/auth/logout/route.ts, src/app/api/auth/user/route.ts, src/app/login/page.tsx]
autonomous: true
requirements: [AUTH-01, AUTH-02]

must_haves:
  truths:
    - "Unauthenticated client requests accessing '/dashboard' or subroutes are redirected to '/login'."
    - "Teachers logging in with valid credentials successfully acquire a session cookie and get redirected."
    - "Database manager handles concurrent table writes atomically without partial write corruption."
  artifacts:
    - path: "src/lib/db.ts"
      provides: "Atomic local file system read/write JSON helpers"
      contains: "fs.rename"
    - path: "src/middleware.ts"
      provides: "Global Route-guard middleware enforcing valid sessions"
      contains: "NextResponse.redirect"
    - path: "src/app/login/page.tsx"
      provides: "Bespoke credential login interface with input feedback"
  key_links:
    - from: "src/middleware.ts"
      to: "/login"
      via: "redirection route check"
      pattern: "redirect.*login"
    - from: "src/lib/db.ts"
      to: "data/classes.json"
      via: "JSON data table persistence checks"
      pattern: "classes\\.json"
---

<objective>
Develop our safe, atomic local JSON database controller, implement secure teacher cookie-based authentication, enforce a global route-guard middleware, and construct a highly premium visual Login Form page.

Purpose: Locks down the data layer and login security so only authenticated teachers can access dashboard attendance sheets.
Output: Active database helper, global middleware redirector, and styled login interface.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-layout/01-CONTEXT.md
@.planning/phases/01-foundation-layout/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Atomic Local JSON Database Manager</name>
  <files>src/lib/db.ts, data/classes.json, data/students.json, data/attendance.json</files>
  <action>
    1. Create `src/lib/db.ts`. Import `fs/promises` and `path` from Node.
    2. Build database controllers: `readTable<T>(tableName: string): Promise<T[]>` and `writeTable<T>(tableName: string, data: T[]): Promise<void>`.
    3. Ensure safe, atomic writes: Write JSON contents into a temporary file first (e.g., `tableName.tmp`), then call `fs.rename` to replace the target table atomically.
    4. Auto-initialize `data/classes.json`, `data/students.json`, and `data/attendance.json` with `[]` if they do not exist.
  </action>
  <verify>Write a small script or test to write records to classes.json and verify they parse back correctly.</verify>
  <done>src/lib/db.ts compiles and manages table arrays reliably with atomic I/O.</done>
</task>

<task type="auto">
  <name>Task 2: Build Teacher Authentication API and Session Configuration</name>
  <files>src/lib/authConfig.ts, src/app/api/auth/login/route.ts, src/app/api/auth/logout/route.ts, src/app/api/auth/user/route.ts</files>
  <action>
    1. Define default credentials in `src/lib/authConfig.ts` (e.g., User: `teacher@aura.edu`, Pass: `Aura123!`).
    2. Write next.js POST handler `src/app/api/auth/login/route.ts` checking credentials. Upon matching, sign a simple JSON payload cookie `aura_session` containing login credentials and expiration time.
    3. Write `logout` API route that deletes the `aura_session` cookie.
    4. Write `user` API route returning current user status.
  </action>
  <verify>Perform local mock fetch requests to the login endpoint to verify set-cookie response headers.</verify>
  <done>Authentication endpoints are functional and correctly manage session cookie cycles.</done>
</task>

<task type="auto">
  <name>Task 3: Develop Global Route-Guard Middleware</name>
  <files>src/middleware.ts</files>
  <action>
    Create `src/middleware.ts` in the root of `src/`. Configure a route matcher targeting private admin workspaces (e.g., `/dashboard/:path*`, `/attendance/:path*`, `/students/:path*`, `/classes/:path*`). Check for the existence and validity of the `aura_session` cookie; redirect unauthorized guests to `/login`.
  </action>
  <verify>Verify that accessing private pages without cookie results in standard redirects.</verify>
  <done>Global session route guard middleware intercepts and secures backend workspace routes.</done>
</task>

<task type="auto">
  <name>Task 4: Build Bespoke Teacher Login UI Page</name>
  <files>src/app/login/page.tsx</files>
  <action>
    Create `src/app/login/page.tsx`. Code a highly premium, centered login card with smooth glassmorphism shadows, light/dark adaptive inputs (with responsive outlines), and clear status error messaging during incorrect submission. Add micro-interactions to form button hover and active states.
  </action>
  <verify>Check component visual alignment and form submission behaviors in browser.</verify>
  <done>Gorgeous, functional administrator login screen is completed and fully integrated.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Direct route access to `/dashboard` redirected to `/login` automatically.
- [ ] Entering bad logins shows dynamic warning alert.
- [ ] Correct login securely authorizes teacher and redirects to `/dashboard`.
</verification>

<success_criteria>
- File-based database layers execute atomic write movements.
- Cookie-based admin logins successfully manage authorization.
- Middleware route guarding is operational.
- Login screen visual style matches modern enterprise standards.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-layout/01-02-SUMMARY.md`
</output>
